<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash screen theme. -->
    <style name="Base.Theme.TruckOrbit.SplashScreen" parent="Theme.SplashScreen">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_round</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.TruckOrbit</item>
    </style>

    <style name="BottomNavigationView.Indicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/transparent</item>
    </style>
</resources>